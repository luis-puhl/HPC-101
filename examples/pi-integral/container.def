Bootstrap: docker
From: ufscar/ubuntu_mpich:latest

%help
    This program computes pi via the integral of:
    4 / (1 + x * x)
    from 0 to 1.
    The value of this integral is pi.
    Based on Tim Mattson (11/99) sequential implementation.

%files
   ./Makefile /opt
   ./pi_seq.c /opt
   ./pi_pth.c /opt
   ./pi_omp.c /opt

%post
    echo "Compiling programs..."
    cd /opt && make
    cp pi_seq pi_pth pi_omp /usr/bin/

%runscript
    exec $@
